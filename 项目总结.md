# imx6ull 复刻工程总结

## 项目简介

基于正点原子 imx6ull 开发板的嵌入式 Qt 模块化项目复刻。原则是**不复制原文件**,
参考原始实现(位于桌面 `imx6ull_project-master`)逐模块重写,目标是系统学习
嵌入式 Linux Qt 开发。

## 技术栈

- Qt 5(QtWidgets;后续步骤将引入 QtMultimedia / QtCharts / Qt5Mqtt)
- 本机 x86 验证:Qt 5.15.2(Anaconda)+ VS2022 MSVC(源码 UTF-8,`/utf-8` 编译)
- 目标平台:imx6ull 开发板 + 正点原子 SDK 交叉编译;开发环境计划 WSL/Linux
- 自动化测试:QtTest(QTest,支持 `QT_QPA_PLATFORM=offscreen` 无头运行)

## 架构设计

- `project.pro` 主工程;后续按模块引入 `form.pri` / `hardware.pri`,加模块只改 .pri
- 主框架 `MainWidget`:左侧 `QListWidget` + 右侧 `QStackedWidget`
- 页面工厂:`PageDescriptor` 结构体保存标题 + 函数指针(lambda 工厂),
  页面延迟创建,避免启动时实例化全部模块;`currentRowChanged` → `setCurrentIndex`
- 7 个规划模块:轮播图 / OpenCV测试 / 摄像头人脸识别 / imx6ull板级设备 /
  音频播放器 / 视频播放器 / MQTT_CLIENT(全部实现)
- MQTT:自研轻量 MQTT 3.1.1 客户端(QoS0,`lib/simplemqtt`),不依赖第三方库
- 硬件层:sysfs/LED、AP3216C、ICM20608 路径均可注入,便于本机测试与板端使用

## 完成进度

- [x] 步骤 1:空工程 — `project.pro` + `main.cpp` + 空白 QWidget(2026-07-31)
- [x] 步骤 2:主框架 — 列表 + 堆栈 + 工厂模式(QtTest 5/5 通过,2026-08-01)
- [x] 步骤 3:轮播图 — 图片定时切换 + 左右按钮 + 圆点指示(2026-08-01)
- [x] 步骤 4:LED — sysfs 读写(`hardware/led`,路径可注入)
- [x] 步骤 5:传感器 — ap3216c 光感 / icm20608 六轴 + 实时曲线(`hardware/` + `form/imx6ulltest`)
- [x] 步骤 6:OpenCV — 图像处理工具集(预处理/滤波/边缘/噪声/分割/变换/特征,OpenCV 3.4.16 x86 验证)
- [x] 步骤 7:摄像头人脸 — VideoCapture + Haar 级联(`hardware/camera` + `form/facedetect`)
- [x] 步骤 8:音视频播放器(QMediaPlayer + 播放列表,`form/musicplay` + `videoplayer`)
- [x] 步骤 9:MQTT 发布/订阅(自研 MQTT 3.1.1 客户端,`form/mqtt_client`)

全部步骤已在本机 x86(MSVC + Qt 5.15.2 + OpenCV 3.4.16)编译运行验证,
自动化测试 12/12 通过;**板端验证仍需在 imx6ull 上完成**。
每步完成标准:在板子上看到实际效果(页面切换/图片轮播/LED亮灭/传感器数据/画面出图)。

## 构建与测试(本机 x86)

```powershell
# 应用(在 x86/ 或任意构建目录)
qmake -spec win32-msvc ..\project\project.pro
nmake

# 测试(subdirs 工程,两个可执行文件)
qmake -spec win32-msvc tests.pro
nmake
$env:QT_QPA_PLATFORM='offscreen'
.\tst_mainwidget\release\tst_mainwidget.exe -o result.txt
.\tst_hardware\release\tst_hardware.exe -o result.txt
```

运行注意事项:
- 运行时需要 `opencv_world3416.dll`(OpenCV `x64/vc15/bin`)在 PATH 或 exe 同目录
- `myMusic` / `myVideo` / `opencv_src`(Haar 模型、示例图)需与 exe 同目录
- OpenCV 路径可在 `project.pro` 中通过 `OPENCV_DIR` 覆盖

## 注意事项

- OpenCV 必须锁死 3.4.x(`types_c.h` 旧接口,4.x 编译不过)
- 先 x86 跑通,再交叉编译上板;板端 OpenCV `.so` 需拷贝或用 `-rpath`
- 上板前先连串口方便看日志,并先在板子 shell 验证 sysfs 通路(LED 等)
- OpenCV 模块中 SVM/字符测试/摄像机标定等演示依赖专用模型与素材,界面中提示跳过
